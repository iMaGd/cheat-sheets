
### Changing Permission

```bash
chmod [-R] u+x,g+wx,o-w /path
```

### Changing Ownership

```bash
# The -R flag is to change files recursively

chown [-R] user /path
chown [-R] user:group /path
chown [-R] user: /path
```
