### List of directories with size and sort
```shell
du -hs * | sort -h
``````

### List of all directories with size
```shell
du -sxh *
ls -al *
```

### List of all files with size
```shell
ls -l --block-size=M
ls -lh
```
